specVersion: "v1beta"
serviceId: "remote_home"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "remote_home-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
